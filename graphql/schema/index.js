const root = require("./root");
const auth = require("./auth");
const video = require("./video");
const comment = require("./comment");
const user = require("./user");

module.exports = [root, auth, video, comment, user];
